import Homepage from "./home.page"
import FilmsPage from "./films.page"
export { Homepage, FilmsPage };

// Import/Export Middleman, this is a single entry point to consolidate my imports into one export, consolidating number of imports I have to do