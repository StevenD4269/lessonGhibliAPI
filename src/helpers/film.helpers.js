// get a list(array) and a string (directors) then return a filtered list of films where only the films by that director are included
export default function filterFilmsByDirector() {
  return (
    <div>Filter Films</div>
  )
}



export default function getListOf() {
  return (
    <div>Get List of Directors</div>
  )
}
